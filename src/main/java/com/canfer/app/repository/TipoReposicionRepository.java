package com.canfer.app.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.canfer.app.model.TipoReposicion;

public interface TipoReposicionRepository extends JpaRepository<TipoReposicion, Long>{
	
}
